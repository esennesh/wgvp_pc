# @package _global_

defaults:
  - override /data: mnist
  - override /guide: auto_normal
  - override /model: vae
  - override /monad: batchvi

monad:
  lr: 1e-3
  local_lr: 1e-3

trainer:
  epochs: 1000
  save_period: 200
