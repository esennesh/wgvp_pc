# @package _global_

defaults:
  - override /data: mnist
  - override /guide: langevin
  - override /model: vae
  - override /monad: batchvi

data:
  shuffle: false
  split_seed: 123

guide:
  lr: 1e-4

monad:
  lr: 1e-2

trainer:
  epochs: 500
  save_period: 250
