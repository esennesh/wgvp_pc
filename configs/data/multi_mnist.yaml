_target_: src.data.multi_mnist.MultiMnistDataModule
data_dir: ${paths.data_dir}
batch_size: 128 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
canvas_side: 50
num_digits: 1
drop_last: true
idx_dim: 1
indexed: true
mutables: true
validation_split: 0.1
num_workers: 0
pin_memory: False
